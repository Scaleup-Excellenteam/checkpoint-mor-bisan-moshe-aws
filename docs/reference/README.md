# Historical reference

`URL_SECURITY_INTEGRATION.patch` is preserved byte-for-byte as a record of an
obsolete integration proposal. Its paths and interfaces describe the old tree;
do not apply it or run its embedded commands. Current setup and commands are in
the [root README](../../README.md).
